@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<WebMvc.Models.ViewModels.VM_Course>

<table class="table">
    <thead>
    <th>name</th>
    <th>Teacher Name</th>
    <th>Department Name</th>
    <th>Actions</th>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.name</td>
                <td>@item.FK_Name1</td>
                <td>@item.FK_Name2</td>
                    <td>
            <a class="btn btn-danger btn-sm" href="/admin/delete?id=@item.Id">Delete</a>
                    <a class="btn btn-warning btn-sm" href="/admin/addcourses?id=@item.Id">Update</a>
                        </td>
            </tr>
        }

    </tbody>
</table>